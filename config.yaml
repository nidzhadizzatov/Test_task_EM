# Contents of the file: /subscription-service/subscription-service/config.yaml

app_name: Subscription Service
version: 1.0.0
port: 8080
database:
  host: localhost
  port: 5432
  user: your_db_user
  password: your_db_password
  name: subscription_service_db
logging:
  level: info
  format: json
  output: stdout
features:
  enable_subscription_notifications: true
  enable_logging: true