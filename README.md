# Subscription Service - Тестовое задание Junior Golang Developer

[![CI](https://github.com/nidzhadizzatov/Test_task_EM/actions/workflows/ci.yml/badge.svg)](https://github.com/nidzhadizzatov/Test_task_EM/actions/workflows/ci.yml)

REST-сервис для агрегации данных об онлайн-подписках пользователей.

## 📋 Описание

Сервис предоставляет API для управления подписками пользователей с возможностью:
- CRUD операции над записями о подписках
- Подсчет суммарной стоимости подписок за период с фильтрацией
- Работа с данными в формате JSON
- Использование PostgreSQL в качестве СУБД



## 🔄 CI/CD

Проект использует GitHub Actions для:
- ✅ Автоматического тестирования
- ✅ Проверки сборки
- ✅ Линтинга кода
- ✅ Сборки Docker образа

## 🚀 Быстрый старт

### Требования
- Go 1.20+
- PostgreSQL 15+
- Docker & Docker Compose (рекомендуется)

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/nidzhadizzatov/Test_task_EM.git
cd Test_task_EM

# Запустите сервисы
docker-compose up --build -d

# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs subscription-service
```

Сервис будет доступен по адресу: `http://localhost:8080`

### Вариант 2: Локальная установка

#### 1. Установка зависимостей
```bash
go mod tidy
```

#### 2. Настройка базы данных
Создайте базу данных PostgreSQL и настройте переменные окружения:

```powershell
# Windows PowerShell
$env:DATABASE_URL = "postgres://user:password@localhost:5432/subscription_service?sslmode=disable"
$env:PORT = "8080"
$env:LOG_LEVEL = "info"
```

#### 3. Запуск миграций
```sql
-- Создайте базу данных
CREATE DATABASE subscription_service;
-- Примените миграцию
\i db/migrations/0001_init.up.sql
```

#### 4. Запуск сервиса
```bash
go run cmd/server/main.go
```

## 📚 API Документация

### Основные endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/health` | Проверка состояния сервиса |
| POST | `/api/v1/subscriptions` | Создание подписки |
| GET | `/api/v1/subscriptions` | Получение всех подписок |
| GET | `/api/v1/subscriptions/:id` | Получение подписки по ID |
| PUT | `/api/v1/subscriptions/:id` | Обновление подписки |
| DELETE | `/api/v1/subscriptions/:id` | Удаление подписки |
| GET | `/api/v1/subscriptions/cost` | Подсчет суммарной стоимости с фильтрацией |

### Примеры запросов

#### 1. Создание подписки (точно как в тестовом задании)
```bash
curl -X POST http://localhost:8080/api/v1/subscriptions \
  -H "Content-Type: application/json" \
  -d '{
    "service_name": "Yandex Plus",
    "price": 400,
    "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
    "start_date": "07-2025"
  }'
```

**Ответ:**
```json
{
  "id": 1,
  "service_name": "Yandex Plus",
  "price": 400,
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "start_date": "07-2025",
  "end_date": null,
  "created_at": "2025-12-04T08:06:57.910117Z",
  "updated_at": "2025-12-04T08:06:57.910117Z"
}
```

#### 2. Подсчет суммарной стоимости с фильтрацией
```bash
# По пользователю
curl "http://localhost:8080/api/v1/subscriptions/cost?user_id=60601fee-2bf1-4721-ae6f-7636e79a0cba"

# По пользователю и сервису
curl "http://localhost:8080/api/v1/subscriptions/cost?user_id=60601fee-2bf1-4721-ae6f-7636e79a0cba&service_name=Yandex Plus"
```

**Ответ:**
```json
{
  "total_cost": 400,
  "period": "all time",
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "subscriptions": [...]
}
```

## 🧪 Тестирование

### Быстрая проверка работоспособности

```powershell
# Health check
Invoke-RestMethod -Uri "http://localhost:8080/health"

# Создание подписки
$body = @{
    service_name = "Yandex Plus"
    price = 400
    user_id = "60601fee-2bf1-4721-ae6f-7636e79a0cba"
    start_date = "07-2025"
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:8080/api/v1/subscriptions" -Method POST -Body $body -ContentType "application/json"

# Расчет стоимости
$cost = Invoke-RestMethod -Uri "http://localhost:8080/api/v1/subscriptions/cost?user_id=60601fee-2bf1-4721-ae6f-7636e79a0cba"
```

### Unit тесты
```bash
go test ./tests/unit/...
go test ./tests/integration/...
```

## 📁 Структура проекта

```
Test_task_EM/
├── cmd/server/             # Точка входа приложения
│   └── main.go
├── internal/
│   ├── api/
│   │   ├── handlers/       # HTTP обработчики
│   │   └── middleware/     # Middleware для логирования
│   ├── config/            # Конфигурация приложения
│   ├── logger/            # Логирование
│   ├── model/             # Модели данных
│   ├── repository/        # Слой работы с БД
│   └── service/           # Бизнес-логика
├── db/migrations/         # SQL миграции
├── api/                   # OpenAPI спецификация (Swagger)
├── tests/                 # Unit и интеграционные тесты
├── docker/                # Docker конфигурация
├── docker-compose.yml     # Docker Compose конфигурация
├── config.yaml           # Конфигурация приложения
├── .env.example          # Пример переменных окружения
├── go.mod                # Go модуль
└── README.md             # Эта документация
```

## 🔧 Конфигурация

### Переменные окружения:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `PORT` - порт для HTTP сервера (по умолчанию 8080)
- `LOG_LEVEL` - уровень логирования (info, debug, error)

### Конфигурационные файлы:
- [config.yaml](http://_vscodecontentref_/0) - основная конфигурация
- [.env.example](http://_vscodecontentref_/1) - пример переменных окружения

## 📋 Особенности реализации

✅ **Архитектурные решения:**
- Clean Architecture с разделением на слои
- Gin веб-фреймворк для высокой производительности
- PostgreSQL с нативным драйвером lib/pq
- Структурированное логирование
- Валидация входных данных

✅ **Соответствие требованиям:**
- CRUD операции над подписками
- Подсчет суммарной стоимости с фильтрацией
- PostgreSQL с миграциями
- Docker Compose развертывание
- OpenAPI/Swagger документация
- Логирование всех операций

## 📖 OpenAPI/Swagger

Полная документация API доступна в файле [openapi.yaml](http://_vscodecontentref_/2).

Для просмотра откройте файл в [Swagger Editor](https://editor.swagger.io/).

## 🚨 Важные особенности

- **Валюта**: Стоимость указывается в рублях как целое число (копейки не учитываются)
- **Формат дат**: MM-YYYY (например, "07-2025")
- **UUID**: User ID должен быть в корректном формате UUID
- **Пользователи**: Проверка существования пользователей не требуется (согласно заданию)

## 🏗️ Технический стек

- **Язык**: Go 1.20+
- **Веб-фреймворк**: Gin
- **База данных**: PostgreSQL 15
- **ORM**: Нативный database/sql + lib/pq
- **Контейнеризация**: Docker & Docker Compose
- **Документация**: OpenAPI 3.0
- **Тестирование**: Go testing + testify

## 👨‍💻 Разработка

### Локальная разработка
```bash
# Установка зависимостей
go mod download

# Запуск в режиме разработки
go run cmd/server/main.go

# Форматирование кода
go fmt ./...

# Линтинг (если установлен golangci-lint)
golangci-lint run
```

### Остановка сервисов
```bash
# Остановка Docker Compose
docker-compose down

# Остановка с удалением volumes
docker-compose down -v
```

---

**Тестовое задание выполнено для позиции Junior Golang Developer в Effective Mobile**